function start() {
    window.location.href = "vales.html";
}

function redeem(type) {
    if (type === 'beijo','testa') {
        window.location.href = "ticket.html"; // Redireciona para a página de resgate do Vale Beijo
    } else {
        // Adicione aqui a lógica para redirecionar para outras páginas de resgate
    }
}
